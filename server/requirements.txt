# TORCH: install the CPU wheels from PyTorch's own index (~990 MB installed, no
# GPU packages), then this file:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# The index-url matters. Without it pip takes torch from PyPI, whose Linux build
# is the CUDA one: it works, but pulls ~3 GB of NVIDIA libraries a GPU-less
# server can never use.
#
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
torch>=2.4
torchvision>=0.19
pydicom>=3.0
pylibjpeg>=2.1
pylibjpeg-libjpeg>=2.4
pylibjpeg-openjpeg>=2.5
opencv-python-headless>=4.9
numpy>=2.0
